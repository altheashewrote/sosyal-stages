const config = {
    theme: {
        extend: {
            fontFamily: {
                lot: ['Lot', 'sans-serif'],
                horizon: ['Horizon', 'sans-serif'],
                helvetica: ['Helvetica', 'Arial', 'sans-serif'],
            }
        }
    }
}

export default config